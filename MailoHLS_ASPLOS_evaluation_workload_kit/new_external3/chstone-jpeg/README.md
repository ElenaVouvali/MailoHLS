# CHStone JPEG
Canonical CHStone JPEG decompression benchmark. The complete benchmark-local source tree used by `main.c`, including the self-contained test vectors in `init.h`, is flattened from pinned upstream blobs before MailoHLS action derivation.
