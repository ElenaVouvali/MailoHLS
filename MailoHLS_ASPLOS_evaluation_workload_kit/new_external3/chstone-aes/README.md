# CHStone AES
Canonical CHStone security benchmark. `materialize_upstream.py` renames only the upstream HLS top `main` to `chstone_aes`, exactly mirroring the upstream Vitis script's `-Dmain=chstone_main` policy, then derives MailoHLS actions from the flattened pinned source.
